
importClass(
	org.eclipse.jetty.webapp.WebAppContext,
	java.io.File)

// The context
/*var context = new WebAppContext()
context.displayName = 'The example web application for the servlet skeleton'
context.contextPath = here
context.tempDirectory = new File(here, 'temporary')
handlers.addHandler(context)

// Handlers
executeAll(new File(here, 'handlers'))*/
